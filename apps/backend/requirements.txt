fastapi>=0.111.0
pytest>=8.0.0
python-multipart>=0.0.9
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # For PostgreSQL (dev only; use psycopg3 in production)
# Note: use "postgresql://user:pass@host/db" for DATABASE_URL
